import React from 'react';

const Dropdown = (props) => {
  return (
    <h1>Dropdown</h1>
    // <ul>
    //   {props.levels?.map((level, i) => (
    //     <li key={i}>{level}</li>
    //   ))}
    // </ul>
  );
};

export default Dropdown;
